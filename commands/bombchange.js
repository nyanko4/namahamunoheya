const { status } = require("../module/bomb");
const { sendchatwork } = require("../ctr/message");

async function bombchange(body, messageId, roomId, messageId) {
  return status.gakusei = true;
}

module.exports = bobmchange;
